services:
  uptimekuma:
    container_name: uptimekuma
    image: louislam/uptime-kuma:1
    restart: unless-stopped
    ports:
      - "3002:3001"
    volumes:
      - /opt/hemmaos/data/uptimekuma:/app/data
    networks:
      - hemmaos

networks:
  hemmaos:
    external: true
